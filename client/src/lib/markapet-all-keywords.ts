// ---------------------------------------------------------------------------
// "Tüm anahtar kelimeler" (broad pet-shop) keyword corpus for MARKA.PET
// (marka.pet, store id "markapet") exclusive SEO landing pages — the 8th corpus
// in the family and the FIRST one built for a CARGO (Türkiye-geneli) store.
//
// The raw keyword universe attached for marka.pet is the same broad pet-shop set
// already shipped as the canonical atakum source, so instead of duplicating an
// 8.5k-line string list we re-export the canonical list under a markapet-named
// symbol. DISTINCTNESS between corpora comes entirely from the GENERATOR
// (keyword-pages-markapet-all.ts), which authors a wholly separate, cargo-framed
// voice (Türkiye geneli kargo, güvenli online ödeme) — never from the keyword
// data. The cargo truthfulness (no same-day / no door-payment / no local
// presence) is enforced in the generator, not here.
// ---------------------------------------------------------------------------

export { ATAKUM_ALL_KEYWORDS as MARKAPET_ALL_KEYWORDS } from "./atakum-all-keywords";
