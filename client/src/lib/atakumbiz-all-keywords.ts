// ---------------------------------------------------------------------------
// "Tüm anahtar kelimeler" (broad pet-shop) keyword corpus for ATAKUM PET
// (atakum.biz, store id "atakumbiz") exclusive SEO landing pages — the 7th
// corpus in the family.
//
// The raw keyword list attached for atakum.biz is identical to the canonical
// atakum source, so instead of shipping a third 8.5k-line copy of the same
// strings we simply re-export the canonical list under an atakumbiz-named
// symbol. The DISTINCTNESS between the corpora comes entirely from the
// GENERATOR (keyword-pages-atakumbiz-all.ts), which authors wholly separate
// prose, headings, FAQ and section rhythm — not from the keyword data.
// ---------------------------------------------------------------------------

export { ATAKUM_ALL_KEYWORDS as ATAKUMBIZ_ALL_KEYWORDS } from "./atakum-all-keywords";
