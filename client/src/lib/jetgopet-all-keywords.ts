// ---------------------------------------------------------------------------
// Keyword source for the JETGO PET (jetgo.pet, store id "jetgopet") broad
// SEO landing-page corpus.
//
// jetgo.pet is a SEPARATE, self-canonical store on the flagship JETGO brand —
// same JETGO brand word + LOCAL same-day Samsun model as jetgomarket.com, but
// its own URL. It reuses the very same broad pet-shop keyword research as the
// Atakum corpus (the queries pet owners actually type are the same), so the
// seed list is the shared ATAKUM_ALL_KEYWORDS array re-exported under a
// jetgopet-scoped name. The corpus's UNIQUENESS lives entirely in the prose of
// keyword-pages-jetgopet-all.ts, never in the seed list.
// ---------------------------------------------------------------------------

export { ATAKUM_ALL_KEYWORDS as JETGOPET_ALL_KEYWORDS } from "./atakum-all-keywords";
