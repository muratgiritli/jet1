// ---------------------------------------------------------------------------
// "Tüm anahtar kelimeler" (broad pet-shop) keyword corpus for JETGO SHOP
// (jetgo.shop, store id "jetgoshop") exclusive SEO landing pages — the 6th
// corpus in the family.
//
// The raw keyword list attached for jetgo.shop
// (attached_assets/ALL_SEO_KEYWORDS_1782038090517.txt) is BYTE-IDENTICAL to the
// atakum source, so instead of shipping a second 8.5k-line copy of the same
// strings we simply re-export the canonical list under a jetgoshop-named symbol.
// The DISTINCTNESS between the two corpora comes entirely from the GENERATOR
// (keyword-pages-jetgoshop-all.ts), which authors wholly separate prose, not
// from the keyword data.
// ---------------------------------------------------------------------------

export { ATAKUM_ALL_KEYWORDS as JETGOSHOP_ALL_KEYWORDS } from "./atakum-all-keywords";
